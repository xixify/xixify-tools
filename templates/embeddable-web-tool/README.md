# Embeddable Web Tool Template

Starter template for client-side HTML/CSS/JS tools.

## Features
- Modular Vanilla JS structure (no build step required)
- Clean, responsive CSS scoped with CSS variables
- Easily embeddable via WordPress iframe or Custom HTML block
